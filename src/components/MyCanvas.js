import React from 'react';

// Componente simples de canvas para renderizações futuras
const MyCanvas = () => {
  return (
    <canvas role="img" className="my-canvas"></canvas>
  );
};

export default MyCanvas;
